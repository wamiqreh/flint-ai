# Python Starter Template

This starter provides a minimal Python project layout for using Flint.

## Use

1. Start API stack:

```bash
docker compose up --build
```

2. Install SDK:

```bash
pip install flint-ai
```

3. Run starter script:

```bash
python app.py
```
